[package]
name = "blockchain"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes-utils = { path = "../bytes" }

actix-web = { workspace = true }
chrono = { workspace = true }
sha2 = { workspace = true }

[dev-dependencies]
rand = { workspace = true }
